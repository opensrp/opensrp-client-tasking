package org.smartregister.tasking.util;

import java.util.Map;

/**
 * Created by Ephraim Kigamba - nek.eam@gmail.com on 27-07-2020.
 */
public class TaskingUtils {

/*
    public static String getGlobalConfig(String key, String defaultValue) {
        Map<String, Object> globalConfigs = RevealApplication.getInstance().getServerConfigs();
        Object val = globalConfigs != null ? globalConfigs.get(key) : null;
        return val == null ? defaultValue : val.toString();
    }

    public static Float getLocationBuffer() {
        return Float.valueOf(getGlobalConfig(TaskingConstants.CONFIGURATION.LOCATION_BUFFER_RADIUS_IN_METRES, BuildConfig.MY_LOCATION_BUFFER + ""));
    }*/
}
